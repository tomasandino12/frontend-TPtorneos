# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Este proyecto usa pnpm (pnpm-lock.yaml) — no el lockfile de npm
package-lock.json

# Variables de entorno (Client ID de Google, VITE_API_URL, etc.) —
# .env.example queda versionado como referencia, .env con los valores
# reales no.
.env
.env.*
!.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

CLAUDE.md

# Salida de los tests automatizados
coverage
playwright-report
test-results
